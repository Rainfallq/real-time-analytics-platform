# Python 3.13 Compatible versions
# Устанавливаем без строгих версий - pip найдет совместимые

# Core FastAPI
fastapi
uvicorn[standard]

# Pydantic - последние версии с поддержкой Python 3.13
pydantic>=2.9
pydantic-settings>=2.5

# Database - asyncpg может не скомпилироваться, используем SQLite для начала
sqlalchemy>=2.0
aiosqlite  # Async SQLite вместо asyncpg для начала
alembic

# Authentication & Security
python-jose[cryptography]
passlib[bcrypt]
python-multipart
bcrypt

# Redis
redis

# Async utilities
httpx
aiofiles

# Utils
python-dotenv
email-validator

# Testing (опционально для старта)
pytest
pytest-asyncio

# Code quality (опционально)
black
ruff